window_width = 500
window_height = 500

pixels_per_unit = 10
pixels_per_print = 50
grid_alpha = 0.1123

local constants = {
    window_width = window_width,
    window_height = window_height,
    pixels_per_print = pixels_per_print,
    pixels_per_unit = pixels_per_unit,
    grid_alpha = grid_alpha
}
return constants
